% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_humidity.R
\name{simulate_rh}
\alias{simulate_rh}
\title{Simulate Relative Humidity Time Series}
\usage{
simulate_rh(
  stations,
  time_index,
  rainfall = NULL,
  temperature = NULL,
  ar_coeff = 0.7,
  seasonal_strength = 8,
  rain_sensitivity = 0.04,
  temp_sensitivity = 0.6,
  coastal_moisture = 12,
  noise_sd = 3,
  min_rh = 15,
  max_rh = 100,
  seed = NULL
)
}
\arguments{
\item{stations}{data.frame from create_stations()}

\item{time_index}{data.frame from generate_time_index()}

\item{rainfall}{Optional rainfall data.frame from
simulate_rainfall().}

\item{temperature}{Optional temperature data.frame
from simulate_temperature().}

\item{ar_coeff}{Numeric.
AR(1) persistence coefficient.
Default = 0.7}

\item{seasonal_strength}{Numeric.
Controls seasonal RH variability.
Default = 8}

\item{rain_sensitivity}{Numeric.
RH increase per rainfall unit.
Default = 0.04}

\item{temp_sensitivity}{Numeric.
RH decrease per temperature unit.
Default = 0.6}

\item{coastal_moisture}{Numeric.
Coastal humidity enhancement factor.
Default = 12}

\item{noise_sd}{Numeric.
Standard deviation of stochastic noise.
Default = 3}

\item{min_rh}{Numeric.
Minimum allowable RH (\%).
Default = 15}

\item{max_rh}{Numeric.
Maximum allowable RH (\%).
Default = 100}

\item{seed}{Optional numeric seed.}
}
\value{
data.frame containing:
\describe{
\item{Station}{Station name}
\item{LON}{Longitude}
\item{LAT}{Latitude}
\item{ELEV}{Elevation}
\item{DATE}{Simulation timestamp}
\item{Year}{Calendar year}
\item{Month}{Calendar month}
\item{Season}{Climatological season}
\item{RH}{Relative humidity (\%)}
\item{Humidity_Anomaly}{Humidity anomaly}
}
}
\description{
Generates synthetic Relative Humidity (RH) series
for multiple climate stations using stochastic
hydro-climatic relationships.
}
\details{
The simulation incorporates:
\itemize{
\item seasonal humidity cycles,
\item rainfall-humidity coupling,
\item temperature-humidity interaction,
\item coastal moisture effects,
\item elevation drying effects,
\item temporal persistence,
\item stochastic atmospheric variability,
\item physically realistic RH bounds.
}

Higher rainfall generally increases RH,
while higher temperature lowers RH.
}
\examples{
stations <- create_stations(
  n = 5,
  seed = 123
)

time_index <- generate_time_index(
  start_date = "2000-01-01",
  end_date = "2005-12-31",
  frequency = "monthly"
)

rain <- simulate_rainfall(
  stations,
  time_index
)

temp <- simulate_temperature(
  stations,
  time_index
)

rh <- simulate_rh(
  stations,
  time_index,
  rainfall = rain,
  temperature = temp
)

head(rh)

}
